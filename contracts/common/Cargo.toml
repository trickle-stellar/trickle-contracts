[package]
name = "trickle-common"
version = "0.1.0"
edition = "2021"
publish = false
description = "Shared types and error enums for Trickle streaming payment contracts"

[lib]
crate-type = ["rlib"]

[dependencies]
soroban-sdk = { workspace = true }
